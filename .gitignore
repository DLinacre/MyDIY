# OS / editor
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node (used only by optional local tooling — not required by the app)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output / caches
dist/
build/
.cache/
*.local

# Logs
*.log
logs/

# Environment / secrets — never commit tokens or credentials
.env
.env.*
!.env.example
.netrc
.git-credentials
